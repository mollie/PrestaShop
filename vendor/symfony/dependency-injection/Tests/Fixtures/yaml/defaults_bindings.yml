services:
    _defaults:
        bind:
            $quz: value
            $foo: [value]

    bar:
        class: MolliePrefix\Symfony\Component\DependencyInjection\Tests\Fixtures\Bar

    foo:
        class: stdClass
